.card
  .card-header.d-flex.align-items-center
    %span User detail 
  .card-body
    %p
      %strong Email:
      = @user.email
    %p
      %strong Full Name:
      = @user.full_name
    %p
      %strong Role:
      = @user.user_role == '0' ? 'User' : @user.user_role == '1' ? 'Poster' : 'Admin'
    %p
      %strong Created at:
      = @user.created_at
    %p
      %strong Updated at:
      = @user.updated_at
    %p
      %strong password:
      ********
    %p
      %strong tags:
      = @user.tags.join(' -')
  .card-footer.d-flex
    = link_to 'Edit', edit_profile_path(@user), class: 'btn btn-outline-secondary'
    = link_to 'Back', posts_path, class: 'btn btn-link ms-auto'
